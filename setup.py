from setuptools import setup, find_packages

setup(
    name="dlc_clustering",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "polars",  # or any other dependencies
    ],
)
